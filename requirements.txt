# MultAI — pinned runtime dependencies
# Generated 2026-04-01 — update by running: pip-audit && pip freeze > requirements.txt

# Core (always installed)
playwright==1.58.0
openpyxl==3.1.5

# Agent fallback (install with: bash setup.sh --with-fallback)
browser-use==0.12.2
anthropic==0.76.0
fastmcp==2.0.0
